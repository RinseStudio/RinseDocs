---
import { twMerge } from 'tailwind-merge';

const { className = null, ...props } = Astro.props;
---

<tr class={twMerge('border-b border-zinc-200 dark:border-zinc-800', className)} {...props}>
  <slot />
</tr>
