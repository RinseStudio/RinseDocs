---
import { twMerge } from 'tailwind-merge';

const { className = null, ...props } = Astro.props;
---

<tfoot class={twMerge('medium color-cool-gray-600 text-xs', className)} {...props}>
  <slot />
</tfoot>
