---
import { twMerge } from 'tailwind-merge';
const { className, ...props } = Astro.props;
---

<div
  class={twMerge(
    'px-2 py-1 text-[13px] font-semibold text-gray-900 dark:text-gray-100',
    className,
  )}
  {...props}
>
  <slot />
</div>
