---
import { twMerge } from 'tailwind-merge';
const {
  className = null,

  ...props
} = Astro.props;
---

<div class="relative" x-on:click="isOpen = !isOpen" x-ref="selectTrigger">
  <slot />
</div>
