---
import { twMerge } from 'tailwind-merge';
const {
  className = null,
  placeholder = 'Select a fruit',
  ...props
} = Astro.props;
---

<div class={twMerge(className)} {...props} set:html={placeholder} />
