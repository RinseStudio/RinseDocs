---
import { twMerge } from 'tailwind-merge';
const { className = null, ...props } = Astro.props;
---

<div x-ref="tooltipTrigger" x-on:mouseenter="isVisible = true" x-on:mouseleave="isVisible = false" class={twMerge(className)} {...props}>
  <slot />
</div>
