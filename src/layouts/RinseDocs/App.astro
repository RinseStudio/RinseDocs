---
import { getCollection } from 'astro:content';
import { SITE } from '@/rinsedocs.config';

import Layout from '@layouts/rinsedocs/Base.astro';

const { frontmatter, entry } = Astro.props;
---

<Layout title={frontmatter.title} pageSettings={frontmatter.pageSettings} frontmatter={frontmatter}>hello world</Layout>
