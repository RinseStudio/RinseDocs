---
title: 'Basic Dropdown'
layout: '@/layouts/rinsedocs/SampleComponentBase.astro'
className: 'mx-auto grid w-full max-w-[320px] grid-cols-1 gap-2 px-5 py-10'
bodyClassName: 'flex items-center justify-center bg-white dark:bg-transparent'
---

import { Button } from '@/components/Button';

<div class="grid grid-cols-2 gap-2">
  <Button variant="outline">Default</Button>
  <Button variant="outline" disabled>
    Disabled
  </Button>
</div>
<div>
  <Button variant="outline" isDanger className="w-full">
    Danger
  </Button>
</div>
<div>
  <Button variant="outline" className="w-full">
    Fluid
  </Button>
</div>

```mdx
import { Button } from '@/components/Button';

<Button variant="outline">Default</Button>
<Button variant="outline" disabled>
  Disabled
</Button>
<Button variant="outline" isDanger className="w-full">
  Danger
</Button>
<Button variant="outline" className="w-full">
  Fluid
</Button>
```
